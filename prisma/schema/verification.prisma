// Required by Better Auth — used to store email-verification codes,
// magic-link tokens and OAuth state during social (Google) sign-in.
model Verification {
  id         String    @id
  identifier String
  value      String
  expiresAt  DateTime
  createdAt  DateTime?
  updatedAt  DateTime?

  @@index([identifier])
}
